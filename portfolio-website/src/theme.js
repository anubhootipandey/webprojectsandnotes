import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#687EFF',
    },
    secondary: {
      main: '#98E4FF',
    },
    background: {
      default: '#000000',
      paper: '#150050',
    },
    text: {
      primary: '#B6FFFA',
      secondary: '#610094',
    },
  },
});

export default theme;
